# Flavors — `shark flavor`

List available compute flavors (Nova).

## Commands

| Command | Description |
|---|---|
| `list` | List all available flavors |

## Example

```bash
shark flavor list
```

Output columns: **ID**, **Name**, **vCPUs**, **RAM (MB)**, **Disk (GB)**.
